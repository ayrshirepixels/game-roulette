import deckyPlugin from "@decky/rollup";

// The Decky preset wires up TypeScript, the React/@decky externals,
// asset handling and the dist/ output. Pass extra Rollup options as the
// first argument if ever needed.
export default deckyPlugin();
